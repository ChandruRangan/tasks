<!doctype html>
<html lang="en">
  <head>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css" integrity="sha384-ggOyR0iXCbMQv3Xipma34MD+dH/1fQ784/j6cY/iJTQUOhcWr7x9JvoRxT2MZw1T" crossorigin="anonymous">
  </head>
  <body> 
      <form action="/update?id=<%=id%>" method="post">
        <div class="form-group">
            <% for(var i=0;i<data.length; i++) { %>
               <input class="form-group"  type="text" name="pname" placeholder="Product Name" value="<%=data[i].product_name%>">
            <%}%>
        </div>
        <div class="form-group">
            <select class="form-group"  name="category" value="<%=data[0].category_name%>">
                <option>category name</option>
            <% for(var i = 0; i <cat.length; i++){ %>
                <option value="<%= cat[i].category_id%>"><%=cat[i].category_name%></option>
            <%}%>
            </select>
        </div>
        <div class="form-group">
            <% for(var i = 0; i <data.length; i++){ %>
                Price <input class="form-group"  type="text" name="price" placeholder="Price" value="<%=data[i].price%>">
            <%}%>
        </div>
         <div>
            <button  type="submit" name="search">Update</button>
        </div>
    </form>
  </body>
</html>