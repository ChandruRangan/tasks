
<!doctype html>
<html lang="en">
  <head>
    <title>Teammember</title>
    <!-- Required meta tags -->
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">

    <!-- Bootstrap CSS -->
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css" integrity="sha384-ggOyR0iXCbMQv3Xipma34MD+dH/1fQ784/j6cY/iJTQUOhcWr7x9JvoRxT2MZw1T" crossorigin="anonymous">
  </head>
  <body>

    <!-- <form action="/find" method="get"> -->
    <div class="container mt-3">
        <div class="form-check">
            <input type="checkbox" class="form-check-input" id="check1" name="option1" value="something" checked>
            <label class="form-check-label" for="check1">Option 1</label>
          </div>
          <div class="form-check">
            <input type="checkbox" class="form-check-input" id="check2" name="option2" value="something">
            <label class="form-check-label" for="check2">Option 2</label>
          </div>

    </div>
      
    
        <!-- <div class="form-group">
          TeamMembers:<select class="form-group" name="TM" multiple>
            <option></option>
            <% for(var i = 0; i <employee.length; i++){ %>
            <option value="<%= employee[0].id%>"><%=employee[0].FullName%></option>
          <%}%>
          </select>
        </div> -->
        </form>

  </body>
</html>

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <form id="myForm">
             <select id="mySelect">
                <option>One</option>
                <option>Two</option>
                <option>Three</option>
             </select>
             <input type="button" onclick="multipleFunc()" value="Select multiple options">
          </form>
          <p>Press CTRL and click above button to select multiple options at once.</p>
          <script>
             function multipleFunc() {
                document.getElementById("mySelect").multiple = true;
             }
          </script>
    

    
</body>
</html>

document.getElementById('submit').onclick = function() {
    var selected = [];
    for (var option of document.getElementById('FullName').options)
    {
        if (option.selected) {
            selected.push(option.value);
        }
    }
    alert(selected);
}



<form id="form2">
    <select width=300 style="width: 350px"
            size="8" multiple>
      <option value='blue'>Blue</option>
      <option value='green'>Green</option>
      <option value='red'>Red</option>
      <option value='yellow'>Yellow</option>
      <option value='orange'>Orange</option>
    </select>
  </form>

  var select = document.getElementById("inputGroupSelect04");
  var input = document.getElementById("input");
  
  select.addEventListener("change", function(event) {
    const selected = document.querySelectorAll('#inputGroupSelect04 option:checked');
    const values = Array.from(selected).map(el => el.value);
    
    input.value = values.join(', ');
  })


  <div class="form-check">
    <input type="checkbox" class="form-check-input" id="check1" name="FullName" value="<%=employee[0].FullName%>" checked>
     <label class="form-check-label" for="check1">Employee1</label>
   </div> 
    <div class="form-check">
     <input type="checkbox" class="form-check-input" id="check2" name="FullName" value="<%=employee[0].FullName%>" >
     <label class="form-check-label" for="check2">Employee2</label>
 <%}%>
   </div>

</div>


<script>
    const btn = document.querySelector('#btn');
    btn.addEventListener('click', (event) => {
        let checkboxes = document.querySelectorAll('input[name="color"]:checked');
        let values = [];
        checkboxes.forEach((checkbox) => {
            values.push(checkbox.value);
        });
        alert(values);
    });    
</script> 


Employee.find((err,empdetails)=>{
    if(!err){
     res.render("Emptable",{employee:empdetails});
    }});

 <!-- <div class="container mt-3"> -->
               <!-- <label for="name">TeamMembers:</label>
              <input type="text"  placeholder="TeamMembers"  name="TM"><br><br>
              <% for(var i = 0; i <employee.length; i++){ %>
              <select class="select" multiple>
                  <option value="<%=employee[0].FullName%>">Employee1</option>
                  <option value="<%=employee[0].FullName%>">Employee2</option>
                  <option value="<%=employee[0].FullName%>">Employee3</option>
                  <option value="<%=employee[0].FullName%>">Employee4</option>
                  <option value="<%=employee[0].FullName%>">Employee5</option>
                  <option value="<%=employee[0].FullName%>">Employee6</option>
                  <option value="<%=employee[0].FullName%>">Employee7</option>
                  <option value="<%=employee[0].FullName%>">Employee8</option>
              </select>
              <%}%> --> 



              team members:xxxxxxxxxxxxxxxxxxxxx


               <!-- <form action="/find" method="post">
                <div class="form-group"> -->

                    <!-- </div>
                </form>  -->



                router.get('/project', function (req, res) {
                    Employee.find((err,empdetails)=>{
                    console.log("siva")
                      res.render('project', { title: 'projecttable',employee:empdetails });
                  });
                  });


                  TeamMembers:<select class="form-group" name="TM"  multiple>
                    <option selected>Enter Employee Name</option>
                    <% for(var i = 0; i <employee.length; i++){ %>
                    <option  value="<%=employee[i].FullName%>"><%=employee[i].FullName%></option>
                   <%}%>
                   </select><br><br>
                 
